<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>contact page</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="css/style.css">
  
</head>
<body>
<%- include('header') %>


<style>
        :root {
            --primary-brown: #8B4513;
            --dark-coffee: #3C2415;
            --light-brown: #D2B48C;
            --gold: #FFD700;
            --cream: #F5F5DC;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Arial', sans-serif;
            color: var(--dark-coffee);
            background-color: var(--cream);
        }

        /* Hero Section */
        .hero-contact {
            background: linear-gradient(135deg, rgba(139, 69, 19, 0.445), rgba(74, 44, 23, 0.9)),
                  url('https://images.unsplash.com/photo-1447933601403-0c6688de566e?auto=format&fit=crop&w=1920&q=80');
      background-size: cover;
      background-position: center;
      background-attachment: fixed;
      height: 90vh;
      display: flex;
      align-items: center;
      justify-content: center;
      color: var(--white);
      position: relative;
      text-align: center;
    }
        

        .hero-content h1 {
            font-size: 4rem;
      font-weight: bold;
      background: linear-gradient(45deg, var(--gold), var(--cream));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
        }

        .hero-content p {
            font-size: 1.5rem;
            color: var(--gold);
            font-weight: 300;
        }

        /* Section Padding */
        .section-padding {
            padding: 80px 0;
        }

        /* Contact Form Styling */
        .contact-form {
            background: white;
            padding: 40px;
            border-radius: 15px;
            box-shadow: 0 15px 35px rgba(0,0,0,0.1);
            border: 3px solid var(--light-brown);
        }

        .contact-form h3 {
            color: var(--primary-brown);
            margin-bottom: 30px;
            font-weight: bold;
        }

        .form-control {
            border: 2px solid var(--light-brown);
            border-radius: 10px;
            padding: 15px;
            font-size: 16px;
            transition: all 0.3s ease;
            background-color: #fafafa;
        }

        .form-control:focus {
            border-color: var(--primary-brown);
            box-shadow: 0 0 0 0.2rem rgba(139, 69, 19, 0.25);
            background-color: white;
        }

        .submit-btn {
            background: linear-gradient(45deg, var(--primary-brown), var(--dark-coffee));
            border: none;
            padding: 15px 40px;
            color: white;
            font-weight: bold;
            border-radius: 50px;
            transition: all 0.3s ease;
            font-size: 18px;
            
            
        }

        .submit-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 25px rgba(139, 69, 19, 0.4);
            background: linear-gradient(45deg, var(--dark-coffee), var(--primary-brown));
        }

        /* Contact Info Cards */
        .contact-info-card {
            background: white;
            padding: 20px 10px;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            text-align: center;
            margin-bottom: 10px;
            border: 2px solid var(--light-brown);
            transition: all 0.3s ease;
        }

        .contact-info-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 20px 40px rgba(0,0,0,0.15);
        }

        .contact-info-card i {
            font-size: 30px;
            color: var(--primary-brown);
            margin-bottom: 20px;
        }

        .contact-info-card h4 {
            color: var(--dark-coffee);
            margin-bottom: 15px;
            font-weight: bold;
        }

        .contact-info-card p {
            color: #666;
            font-size: 16px;
        }

        /* Map Section */
        .map-container {
            border-radius: 15px;
            overflow: hidden;
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
            height: 400px;
        }

        .map-container iframe {
            width: 100%;
            height: 100%;
            border: none;
        }

        /* Opening Hours */
        .hours-card {
            background: var(--primary-brown);
            color: white;
            padding: 40px;
            border-radius: 15px;
            box-shadow: 0 15px 35px rgba(0,0,0,0.2);
        }

        .hours-card h3 {
            color: var(--gold);
            margin-bottom: 30px;
            text-align: center;
            font-weight: bold;
        }

        .hours-list {
            list-style: none;
            padding: 0;
        }

        .hours-list li {
            padding: 12px 0;
            border-bottom: 1px solid rgba(255,255,255,0.2);
            display: flex;
            justify-content: space-between;
            font-size: 16px;
        }

        .hours-list li:last-child {
            border-bottom: none;
        }

        .day {
            font-weight: bold;
        }

        .time {
            color: var(--gold);
        }

        /* Social Media */
        .social-icons {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-top: 30px;
        }

        .social-icons a {
            width: 60px;
            height: 60px;
            background: var(--primary-brown);
            color: white;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 24px;
            transition: all 0.3s ease;
            text-decoration: none;
        }

        .social-icons a:hover {
            background: var(--gold);
            color: var(--dark-coffee);
            transform: translateY(-5px) scale(1.1);
        }

        /* Newsletter Section */
        .newsletter-section {
            background: linear-gradient(135deg, var(--dark-coffee), var(--primary-brown));
            color: white;
            padding: 60px 0;
        }

        .newsletter-form {
            max-width: 600px;
            margin: 0 auto;
        }

        .newsletter-input {
            border: none;
            padding: 15px 20px;
            border-radius: 50px;
            font-size: 16px;
            margin-right: 10px;
            flex: 1;
        }

        .newsletter-btn {
            background: var(--gold);
            color: var(--dark-coffee);
            border: none;
            padding: 15px 30px;
            border-radius: 50px;
            font-weight: bold;
            transition: all 0.3s ease;
        }

        .newsletter-btn:hover {
            background: white;
            transform: scale(1.05);
        }

        /* Success Message */
        .success-message {
            background: #d4edda;
            color: #155724;
            padding: 15px;
            border-radius: 10px;
            margin-top: 20px;
            display: none;
            border: 1px solid #c3e6cb;
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .hero-content h1 {
                font-size: 2.5rem;
            }
            
            .hero-content p {
                font-size: 1.2rem;
            }
            
            .contact-form {
                padding: 25px;
            }
            
            .newsletter-form {
                flex-direction: column;
            }
            
            .newsletter-input {
                margin-bottom: 15px;
                margin-right: 0;
            }
        }

        /* Animation Classes */
        .fade-in-up {
            opacity: 0;
            transform: translateY(30px);
            animation: fadeInUp 0.8s ease forwards;
        }

        @keyframes fadeInUp {
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>
</head>
<body>
    <!-- Hero Section -->
    <section class="hero-contact">
        <div class="container">
            <div class="hero-content animate__animated animate__fadeInDown">
                <h1> Contact Us</h1>
                <p>We'd love to hear from you!</p>
                <p class="mt-3" style="font-size: 1.2rem; color: var(--cream);">
                    Get in touch for reservations, events, or just to say hello
                </p>
            </div>
        </div>
    </section>

    <!-- Contact Form & Info Section -->
    <section class="section-padding">
        <div class="container">
            <div class="row">
                <!-- Contact Form -->
                <div class="col-lg-8 mb-5">
                    <div style="padding: 60px;"  class="contact-form fade-in-up ">
                         <center><h3><i class="fas fa-envelope"></i> Send us a Message</h3></center>
                        <form id="contactForm">
                            <div class="row">
                                <div class="col-md-6 mb-3 mt-4">
                                    <input type="text" class="form-control" name="name" placeholder="Your Name *" required>
                                </div>
                                <div class="col-md-6 mb-3 mt-4">
                                    <input type="email" class="form-control" name="email" placeholder="Your Email *" required>
                                </div>
                            </div>
                            <div class="row">
                                <div class="col-md-6 mb-3 mt-4">
                                    <input type="tel" class="form-control" name="phone" placeholder="Phone Number (Optional)">
                                </div>
                                <div class="col-md-6 mb-3 mt-4">
                                    <select class="form-control" name="subject" required>
                                        <option value="">Select Subject *</option>
                                        <option value="General Inquiry">General Inquiry</option>
                                        <option value="Reservation">Reservation</option>
                                        <option value="Event Booking">Event Booking</option>
                                        <option value="Feedback">Feedback</option>
                                        <option value="Partnership">Partnership</option>
                                    </select>
                                </div>
                            </div>
                            <div class="mb-4">
                                <textarea class="form-control" name="message" rows="6" placeholder="Your Message *" required></textarea>
                            </div>
                            <center>
                            <button type="submit" class="submit-btn">
                                <i class="fas fa-paper-plane"></i> Send Message
                            </button></center>
                        </form>
                        <div class="success-message" id="successMessage">
                            <i class="fas fa-check-circle"></i> Thank you! Your message has been sent successfully. We'll get back to you soon!
                        </div>
                    </div>
                </div>

                <!-- Contact Info -->
                <div class="col-lg-4">
                    <div class="contact-info-card fade-in-up" style="animation-delay: 0.2s;">
                        <i class="fas fa-map-marker-alt"></i>
                        <h4>Visit Our Cafe</h4>
                        <p>123 Coffee Street<br>Brewtown, CA 90210<br>United States</p>
                    </div>

                    <div class="contact-info-card fade-in-up" style="animation-delay: 0.4s;">
                        <i class="fas fa-phone"></i>
                        <h4>Call Us</h4>
                        <p>+1 (234) 567-8901<br>+1 (234) 567-8902</p>
                    </div>

                    <div class="contact-info-card fade-in-up" style="animation-delay: 0.6s;">
                        <i class="fas fa-envelope"></i>
                        <h4>Email Us</h4>
                        <p>info@brewmastercafe.com<br>events@brewmastercafe.com</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Map & Hours Section -->
    <section class="section-padding" style="background-color: white;">
        <div class="container">
            <div class="row">
                <!-- Map -->
                <div class="col-lg-8 mb-5">
                    <h3 class="mb-4" style="color: var(--primary-brown); font-weight: bold;">
                        <i class="fas fa-map"></i> Find Us Here
                    </h3>
                    <div class="map-container">
                        <iframe 
                            src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d3048.398905993!2d-122.41941542404359!3d37.77492917975903!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x8085808c3a0a6c57%3A0x4a501367f076adff!2sSan%20Francisco%2C%20CA%2C%20USA!5e0!3m2!1sen!2s!4v1703123456789!5m2!1sen!2s"
                            allowfullscreen=""
                            loading="lazy">
                        </iframe>
                    </div>
                </div>

                <!-- Opening Hours -->
                <div class="col-lg-4">
                    <div class="hours-card">
                        <h3><i class="fas fa-clock"></i> Opening Hours</h3>
                        <ul class="hours-list">
                            <li>
                                <span class="day">Monday</span>
                                <span class="time">8:00 AM - 10:00 PM</span>
                            </li>
                            <li>
                                <span class="day">Tuesday</span>
                                <span class="time">8:00 AM - 10:00 PM</span>
                            </li>
                            <li>
                                <span class="day">Wednesday</span>
                                <span class="time">8:00 AM - 10:00 PM</span>
                            </li>
                            <li>
                                <span class="day">Thursday</span>
                                <span class="time">8:00 AM - 10:00 PM</span>
                            </li>
                            <li>
                                <span class="day">Friday</span>
                                <span class="time">8:00 AM - 11:00 PM</span>
                            </li>
                            <li>
                                <span class="day">Saturday</span>
                                <span class="time">9:00 AM - 11:00 PM</span>
                            </li>
                            <li>
                                <span class="day">Sunday</span>
                                <span class="time">9:00 AM - 10:00 PM</span>
                            </li>
                        </ul>
                        
                        <div class="text-center mt-4">
                            <p style="color: var(--gold); font-weight: bold;">
                                <i class="fas fa-info-circle"></i> Special Holiday Hours May Apply
                            </p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Social Media Section -->
    <section class="section-padding" style="background-color: var(--cream);">
        <div class="container text-center">
            <h2 class="mb-4" style="color: var(--primary-brown); font-weight: bold;">
                <i class="fas fa-heart"></i> Follow Us for Updates & Offers
            </h2>
            <p class="lead mb-4" style="color: var(--dark-coffee);">
                Stay connected with BrewMaster Cafe for the latest coffee news, special offers, and behind-the-scenes content!
            </p>
            
            <div class="social-icons">
                <a href="#" title="Facebook">
                    <i class="fab fa-facebook-f"></i>
                </a>
                <a href="#" title="Instagram">
                    <i class="fab fa-instagram"></i>
                </a>
                <a href="#" title="Twitter">
                    <i class="fab fa-twitter"></i>
                </a>
                <a href="#" title="TikTok">
                    <i class="fab fa-tiktok"></i>
                </a>
                <a href="#" title="YouTube">
                    <i class="fab fa-youtube"></i>
                </a>
                <a href="#" title="LinkedIn">
                    <i class="fab fa-linkedin-in"></i>
                </a>
            </div>
            
            <div class="mt-4">
                <p style="color: var(--primary-brown); font-weight: bold;">
                    @brewmastercafe | #BrewMasterCoffee | #CoffeeLovers
                </p>
            </div>
        </div>
    </section>

    <!-- Newsletter Section -->
    <section class="newsletter-section">
        <div class="container text-center">
            <h2 class="mb-3"><i class="fas fa-newspaper"></i> Stay In The Loop</h2>
            <p class="lead mb-4">Subscribe to get exclusive offers, coffee news, and brewing tips delivered to your inbox!</p>
            
            <div class="newsletter-form">
                <form class="d-flex" id="newsletterForm">
                    <input type="email" class="newsletter-input" placeholder="Enter your email address" required>
                    <button type="submit" class="newsletter-btn">
                        <i class="fas fa-bell"></i> Subscribe
                    </button>
                </form>
                <p class="mt-3" style="font-size: 14px; opacity: 0.8;">
                    <i class="fas fa-lock"></i> We respect your privacy. Unsubscribe at any time.
                </p>
            </div>
        </div>
    </section>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/js/bootstrap.bundle.min.js"></script>
    <script>
        // Contact Form Submission
        document.getElementById('contactForm').addEventListener('submit', function(e) {
            e.preventDefault();
            
            // Show success message
            document.getElementById('successMessage').style.display = 'block';
            
            // Reset form
            this.reset();
            
            // Hide success message after 5 seconds
            setTimeout(function() {
                document.getElementById('successMessage').style.display = 'none';
            }, 5000);
        });

        // Newsletter Form Submission
        document.getElementById('newsletterForm').addEventListener('submit', function(e) {
            e.preventDefault();
            
            // Show alert
            alert('Thank you for subscribing! Check your email for confirmation.');
            
            // Reset form
            this.reset();
        });

        // Add fade-in animation on scroll
        function animateOnScroll() {
            const elements = document.querySelectorAll('.fade-in-up');
            
            elements.forEach(element => {
                const elementTop = element.getBoundingClientRect().top;
                const elementVisible = 150;
                
                if (elementTop < window.innerHeight - elementVisible) {
                    element.style.opacity = '1';
                    element.style.transform = 'translateY(0)';
                }
            });
        }

        // Initialize animations
        window.addEventListener('scroll', animateOnScroll);
        window.addEventListener('load', animateOnScroll);

        // Smooth scrolling for any internal links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function(e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });

        // Form validation enhancement
        const inputs = document.querySelectorAll('.form-control');
        inputs.forEach(input => {
            input.addEventListener('blur', function() {
                if (this.value.trim() !== '') {
                    this.style.borderColor = 'var(--primary-brown)';
                    this.style.backgroundColor = '#f8f9fa';
                } else if (this.hasAttribute('required')) {
                    this.style.borderColor = '#dc3545';
                }
            });

            input.addEventListener('focus', function() {
                this.style.borderColor = 'var(--primary-brown)';
                this.style.backgroundColor = 'white';
            });
        });

        // Add loading effect to submit button
        document.getElementById('contactForm').addEventListener('submit', function(e) {
            const submitBtn = this.querySelector('.submit-btn');
            const originalText = submitBtn.innerHTML;
            
            submitBtn.innerHTML = '<i class="fas fa-spinner fa-spin"></i> Sending...';
            submitBtn.disabled = true;
            
            setTimeout(function() {
                submitBtn.innerHTML = originalText;
                submitBtn.disabled = false;
            }, 2000);
        });
    </script>





   
<%- include('footer') %>  
    
    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/js/bootstrap.bundle.min.js"></script>

</body>
</html>