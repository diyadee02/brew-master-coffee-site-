<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login - BrewMaster Cafe</title>
  <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
  <link rel="stylesheet" href="css/style.css">
  <style>
    body {
      background: linear-gradient(135deg, var(--cream) 60%, var(--gold) 100%);
      min-height: 100vh;
      font-family: 'Georgia', serif;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    .login-container {
      background: #fff;
      border-radius: 20px;
      box-shadow: 0 8px 32px rgba(139,69,19,0.15);
      padding: 48px 36px 32px 36px;
      max-width: 400px;
      width: 100%;
      border: 2px solid var(--gold);
      position: relative;
      overflow: hidden;
    }
    .login-title {
      color: var(--coffee-brown);
      font-weight: bold;
      margin-bottom: 32px;
      text-align: center;
      font-size: 2rem;
      letter-spacing: 1px;
    }
    .form-label {
      color: var(--dark-coffee);
      font-weight: 600;
      margin-bottom: 6px;
    }
    .form-control {
      border-radius: 10px;
      border: 2px solid var(--gold);
      margin-bottom: 20px;
      background-color: var(--cream);
      color: var(--dark-coffee);
      font-weight: 500;
      box-shadow: none;
      transition: border-color 0.2s;
    }
    .form-control:focus {
      border-color: var(--coffee-brown);
      box-shadow: 0 0 0 0.2rem rgba(139,69,19,.15);
      background-color: var(--cream);
      color: var(--dark-coffee);
    }
    .btn-login {
      background: linear-gradient(90deg, var(--gold) 70%, var(--coffee-brown) 100%);
      color: var(--cream);
      font-weight: bold;
      border: none;
      border-radius: 10px;
      width: 100%;
      padding: 12px;
      margin-bottom: 16px;
      font-size: 1.1rem;
      box-shadow: 0 4px 16px rgba(218,165,32,0.10);
      transition: background 0.2s, color 0.2s;
    }
    .btn-login:hover {
      background: var(--coffee-brown);
      color: var(--gold);
    }
    .register-link {
      text-align: center;
      display: block;
      color: var(--coffee-brown);
      margin-top: 14px;
      font-weight: 600;
      text-decoration: underline;
      transition: color 0.2s;
    }
    .register-link:hover {
      color: var(--gold);
    }
    .login-icon {
      display: flex;
      justify-content: center;
      align-items: center;
      margin-bottom: 18px;
    }
    .login-icon i {
      font-size: 2.5rem;
      color: var(--gold);
      background: var(--cream);
      border-radius: 50%;
      padding: 10px;
      box-shadow: 0 2px 8px rgba(218,165,32,0.10);
    }
  </style>
</head>
<body>
  <div class="login-container">
    <div class="login-icon">
      <i class="fas fa-mug-hot"></i>
    </div>
    <h2 class="login-title">Sign In to BrewMaster Cafe</h2>
    <form action="/login" method="POST" autocomplete="on">
      <label for="username" class="form-label">Username</label>
      <input type="text" id="username" name="username" class="form-control" required autocomplete="username">
      <label for="password" class="form-label">Password</label>
      <input type="password" id="password" name="password" class="form-control" required autocomplete="current-password">
      <button type="submit" class="btn btn-login"><i class="fas fa-sign-in-alt"></i> Login</button>
    </form>
    <a href="/register" class="register-link">Don't have an account? Register here</a>
  </div>
</body>
</html>
